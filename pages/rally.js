import Layout from "./../components/Layout";

const Rally = (props) => {
  return (
    <Layout menu={props.menu} title="Rally Page">
      <h1>Rally Page</h1>
    </Layout>
  );
};

export default Rally;
